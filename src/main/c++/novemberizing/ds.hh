#ifndef   __NOVEMBERIZING__DS__HH__
#define   __NOVEMBERIZING__DS__HH__

#define foreach(begin, end, block)              \
    for(auto it = begin; it!=end;it++){         \
        block;                                  \
    }

#endif // __NOVEMBERIZING__DS__HH__
